export enum CarHeight {
  Any,
  Low,
  High
}

export enum Companion {
  Alone,
  WithRelative,
  WithHelper,
  WithCompanion,
  WithPet,
  WithChild
}

export enum Follow {
  No,
  AtDesitnation,
  AtPickup,
  Both
}

export enum HelpingUtil {
  None,
  WalkingStick,
  Crutches,
  Rollator,
  Wheelchair,
}

export enum Role {
  Citizen,
  Admin,
}